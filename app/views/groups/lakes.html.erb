<!-- <img src="https://images.unsplash.com/photo-1468549940493-46152524296c?ixlib=rb-0.3.5&q=80&fm=jpg&crop=entropy&s=22fd0ceed4703bb1404e4f447d921041" id="lakepic"> -->

<h1> Lakes Group </h1>

<%= form_tag('/groups/lakes', class: "waves-effect waves-light btn") do %>
  
<%= hidden_field_tag 'user_id', 'current_user.id' %>

<%= submit_tag ('join group'), data: { confirm: 'You are now apart of the Lakes Group' } %>

<% end %>

<div class="container">
    <%= will_paginate @user_paginator %>
  </div>
<% @lakes.users.each do |user| %>
  <div class="divider"></div>
  <div class="section row">
    
    <div class="col m6 offset-m1 s6">
      
      <img src="<%= user.picture %>" class="user-img" alt="user image">

        <div class="container user-ctn">
          <div class="user-info">
          <!-- this should link to specific user's page (user_path) -->
       		<span><%= link_to(user.name, user_path(user)) %></span>    
            <span>| <%= user.city %></span>
            <span>| <%= user.gender %> | </span>
        
          <p>Skill Level: <%= user.skill_level %></p>
          <div class="truncate"><%= user.more_info %></div>
        </div>
       </div>
    </div>
 <% end %>